1. Note that for TLS, the client uses the public key that generated by server to
encryption plaintext. But does it a long time for the calculation process of
encryption if the size of plaintext is huge?
